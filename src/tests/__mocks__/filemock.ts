// Mock for static assets (images, fonts, etc.) used in tests
// Return a simple string so imports like `import img from './x.png'` resolve
const fileMock = "test-file-stub";
export default fileMock;
